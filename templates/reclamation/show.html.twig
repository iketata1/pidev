{% extends 'back_home/basee.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Iduser</th>
                <td>{{ reclamation.iduser }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reclamation.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}" type="button" class="btn btn-info">back to list</a>

   
    <a  type="button" class="btn btn-danger" href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">edit</a>
   
    <a  type="button" class="btn btn-light" href="{{ path('app_reclamation_pdf', {'id': reclamation.id}) }}">imprimer</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
